name: Secret Scan Stub

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - 'release/**'

jobs:
  secret-scan:
    name: Secret scan placeholder
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Placeholder secret scan step
        run: |
          echo "Secret scanning placeholder"
          echo "Future implementation should use approved GitHub or CPA platform scanning controls"
